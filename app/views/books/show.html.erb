<div class=container px-5 px-sm-0 >
<div class="row" >
<div class="col-md-3" >


  <%= render "home",user: @user, book: @book_new %>

</div>

  <div class="col-md-8 offset-md-1">
    <h2>Book detail</h2>

     <table class="table">
      <tbody>
       <tr>
        <td><%= link_to user_path(@book.user) do %><%= image_tag @book.user.get_profile_image(100,100) %><br><%=@book.user.name %><% end %></td>
        <td>
          <% if @book.user == current_user %>
         <%= link_to "Edit", book_path(@book), method: :edit %>
         <% end %>  
        </td>
        <td> 
         <% if @book.user == current_user %>
         <%= link_to "Destroy", book_path(@book), method: :delete %>
         <% end %>
        </td>
        </td>
       </tr>
      </tbody>
     </table>
  </div>

  <div>
 

  </div>
  </div>
  </div>